---
description: "[DEPRECATED] Use /team instead — Agent Teams are native to Claude Code with peer messaging and shared task lists."
---

# /orchestra — DEPRECATED

> This workflow has been replaced by `/team`. Use `/team <composition> <task>` instead.

See `docs/multi-agent-protocol.md` for the multi-agent decision tree.
