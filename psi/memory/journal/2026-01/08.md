## 21:33 - Feature Request: Voice Greetings

**User wants:**
- Matrix should greet when user says "I'm back in" or returns
- Oracle should awaken/speak on session start
- System should voice out on /unplug (farewell)
- This has been discussed multiple times but not implemented

**Action needed:** Implement session boundary voice triggers

## 22:09 - Voice Queue Implemented (Phase 1)

**Achievements:**
- Implemented `safe_play`, `safe_speak`, and `safe_play_agentvibes` in `voice_module.sh`.
- Used `flock -x` on `/var/tmp/matrix_voice.lock` for sequential playback.
- Solved AgentVibes blocking issue with "Generate-Then-Play" strategy (`AGENTVIBES_NO_PLAYBACK=true`).
- Verified with user: Audio is audible and queued correctly.

**Next:** Phase 2 (HTTP API)

## 22:15 - Dynamic Greetings Implemented (Issue #7)

**Achievements:**
- Added "System" voice acknowledgement at session start.
- Implemented randomized Oracle greetings (8 variations).
- Verified queue handles the concurrent startup requests perfectly.
